{
  "name": "ai-forecast-calendar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --test",
    "test": "node --test",
    "validate:data": "node scripts/validate-canonical.mjs data/forecasts/ai-2027.json",
    "validate:ics": "node scripts/validate-ics.mjs dist/calendars/ai-2027.ics",
    "smoke": "node scripts/smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
