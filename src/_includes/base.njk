<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="Published AI forecast milestones, placed on a calendar.">
    <title>{{ title or "AI Forecast Calendar" }}</title>
    <link rel="stylesheet" href="{{ '/styles.css' | url }}">
  </head>
  <body>
    <main>{{ content | safe }}</main>
  </body>
</html>
